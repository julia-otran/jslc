export const ROUTER_PATHS_PARAMS = {
  CTRL_PAGE: (index: number): string => `/ctrl/${index}`,
};

export const ROUTER_PATHS = {
  CTRL_ROOT: '/ctrl',
  CTRL_PAGE: '/ctrl/:index',
  IO_SETUP: '/io-setup',
};
